"use strict"; // treat all the js code newer version

// alert(3+3) ( if we are using nodejs it is showing error, but if we use broswer it show popup 6 ).

let name = "Abhisek"
let age = 23
let isLogged = false

// Primitive Datatype

/*
number => 2 to power 53
bigint
string => ""
boolean
null => standalone value (empty value).
undefined => (variable is declared but the variable having no value).
symbol => unique
*/

// Non primitive Datatype

/*
object

*/

console.log(typeof name);
console.log(typeof age);
console.log(typeof null); // null is an object
console.log(typeof undefined);
